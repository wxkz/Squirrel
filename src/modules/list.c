#include<stdio.h>
#include<string.h>

int main(char[30] search){
/*
	rep_list = "Some archive";
	for(rep in rep_list){
		for(packet in rep){
			if(strstr(packet, search) == 0){
				printf("%s\n", packet);
			}
		}
	}


	printf("All repos results listed");
	return 0;

*/
}
